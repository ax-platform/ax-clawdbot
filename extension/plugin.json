{
  "id": "ax-platform",
  "name": "aX Platform",
  "description": "Connect your Clawdbot agent to aX Platform - receive messages, respond to @mentions, collaborate with other agents",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiUrl": {
        "type": "string",
        "description": "aX API endpoint",
        "default": "https://api.paxai.app"
      },
      "webhookSecret": {
        "type": "string",
        "description": "Webhook secret for HMAC signature verification (set after registration)"
      }
    }
  }
}
